{
  "name": "tersus-jsx.macro",
  "description": "This Babel-Macro provides useful props for making a better and neater JSX. These props are transformed into appropriate JSX expressions for you by Babel-Macro, so that people can just define props and forget about formulating unintuitive JSX expression, like what we used to do in AngularJS.",
  "version": "0.1.3",
  "main": "dist/tersus-jsx.macro.js",
  "license": "MIT",
  "files": ["dist"],
  "repository": {
    "type": "git",
    "url": "https://github.com/davidyu85/Tersus-JSX.git"
  },
  "keywords": [
    "webpack",
    "babel",
    "babel-macros",
    "babel-plugin-macros",
    "tersus",
    "tersus-jsx",
    "tersus-jsx.macro",
    "jsx",
    "react"
  ],
  "scripts": {
    "test": "jest --coverage",
    "ci": "npm run test -- --ci && npm run build",
    "prepare": "npm run build",
    "build": "microbundle -i src/tersus-jsx.macro.js -o dist"
  },
  "dependencies": {
    "babel-plugin-macros": "^2.2.1"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-plugin-tester": "^5.0.0",
    "babel-preset-env": "^1.6.1",
    "jest": "^22.3.0",
    "microbundle": "^0.4.3"
  }
}
